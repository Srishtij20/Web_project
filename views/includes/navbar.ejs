<nav class="navbar navbar-expand-md bg-white border-bottom sticky-top shadow-sm">
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-sun text-warning fs-4"></i>
      <span style="font-size:1.15rem; letter-spacing:-0.3px;">Solara</span>
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNav">
      <form class="d-flex mx-auto my-2 my-md-0 search-form" role="search" action="/listings" method="GET">
        <div class="input-group search-group">
          <input
            class="form-control search-inp border-end-0"
            type="search"
            name="search"
            value="<%= typeof search !== 'undefined' ? search : '' %>"
            placeholder="Search destinations..."
          />
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>
      </form>
      <ul class="navbar-nav ms-auto align-items-center gap-1 mt-2 mt-md-0">
        <li class="nav-item">
          <a class="nav-link px-3 py-2 rounded-pill host-btn" href="/listings/new">
            <i class="fa-solid fa-plus me-1"></i>List your property
          </a>
        </li>
        <% if (!currUser) { %>
          <li class="nav-item"><a class="nav-link" href="/signup"><b>Sign up</b></a></li>
          <li class="nav-item"><a class="btn btn-dark rounded-pill px-3" href="/login">Log in</a></li>
        <% } else { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center gap-2 border rounded-pill px-3 py-2"
               href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-bars"></i>
              <div class="user-avatar"><%= currUser.username.charAt(0).toUpperCase() %></div>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2">
              <li>
                <span class="dropdown-item-text text-muted small px-3">
                  Signed in as <b><%= currUser.username %></b>
                </span>
              </li>
              <li><hr class="dropdown-divider my-1"></li>
              <li><a class="dropdown-item" href="/listings/wishlist"><i class="fa-regular fa-heart me-2 text-danger"></i>Wishlist</a></li>
              <li><a class="dropdown-item" href="/listings/new"><i class="fa-solid fa-plus me-2"></i>Add listing</a></li>
              <li><hr class="dropdown-divider my-1"></li>
              <li><a class="dropdown-item text-danger" href="/logout"><i class="fa-solid fa-right-from-bracket me-2"></i>Log out</a></li>
            </ul>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
